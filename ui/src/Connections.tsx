import { Text, Title } from '@mantine/core';

export default function Connections() {
  return (
    <>
      <Title order={2}>Connections</Title>
      <Text c="dimmed" mt="sm">
        iTop / LLM / security / ticket mapping settings will appear here (plan step 3).
      </Text>
    </>
  );
}
