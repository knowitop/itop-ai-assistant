import { Text, Title } from '@mantine/core';

export default function Runs() {
  return (
    <>
      <Title order={2}>Runs</Title>
      <Text c="dimmed" mt="sm">
        Processing-run monitoring will appear here (plan step 4).
      </Text>
    </>
  );
}
