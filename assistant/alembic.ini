# Dev CLI convenience: `uv run alembic upgrade head` from assistant/.
# The URL is resolved by env.py: ALEMBIC_DATABASE_URL env var, or the app's
# DATABASE_URL (.env / env vars). At runtime the app applies migrations
# itself on startup (see vector/db.py run_migrations).
[alembic]
script_location = src/vector/migrations
