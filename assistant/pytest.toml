[pytest]
addopts = [
    "--import-mode=importlib",
    "--cov=itop_ai_assistant",
    "--cov-report=term-missing",
]
testpaths = [
    "test/unit"
]
asyncio_mode = "auto"
log_cli = true
log_cli_level = "INFO"
log_cli_format = "%(name)s: %(message)s"