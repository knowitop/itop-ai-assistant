# Non-secret, environment-independent defaults only. Connection endpoints,
# credentials and the model name are environment-specific — set them in .env
# (see docker/.env.dist) or at runtime through the setup API, not here.
app_host: "0.0.0.0"
app_port: 8000
log_level: "INFO"

redis_url: "redis://localhost:6379"
